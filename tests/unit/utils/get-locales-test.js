import getLocales from 'ember-cli-i18n-locales/utils/get-locales';
import { module, test } from 'qunit';

module('getLocales');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getLocales();
  assert.ok(result);
});
